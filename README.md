GeDi can be run only on a linux machine with x86-64 architecture. 

Installation of GeDi requires the following dependencies:
- make
- cmake
- g++ (version 7.3.0 with OpenMP)

To install GeDi run command:
bash install.sh

